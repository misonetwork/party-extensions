[package]
name = "party_platform_link"
edition = "2024"
license = "Apache 2.0"
authors = ["Miso Labs, Inc. (build@miso.network)"]

[dependencies]
miso_party = { local = "../../../misonetwork/party" }
platform_link = { local = "../lib/platform_link" }

# Test-only: a concrete platform payload to instantiate the generics in tests.
# `modes = ["test"]` keeps it out of the production dependency graph.
party_social = { local = "../party_social", modes = ["test"] }
