[package]
name = "party_genre"
edition = "2024"
license = "Apache 2.0"
authors = ["Miso Labs, Inc. (build@miso.network)"]

[dependencies]
miso_party = { local = "../../miso-party" }
genre = { git = "https://github.com/misonetwork/miso-protocol-extensions.git", subdir = "lib/genre", rev = "main" }
